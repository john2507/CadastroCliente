{{#each erros}}

<div class="alert alert-danger">{{texto}}</div>

{{else}}

{{/each}}

<h3>Editar Clientes</h3>
 <div class="card">
     <div class="card-body">
        <form action="/admin/usuarios/edit" method="POST">

       <input type="hidden" name="id" value="{{usuarios._id}}">
        
            <label for="nome">Nome Cliente</label>
                <input type="text" id="nome" name="nome" placeholder="Nome do Cliente" class="form-control" value="{{usuarios.nome}}">
            
              <label for="endereco">Endereco Cliente</label>
                <input type="text" id="endereco" name="endereco" placeholder="Ddigite o Endereço do Cliente" class="form-control" value="{{usuarios.endereco}}">
            
            <label for="email">Email Cliente</label>
                <input type="email" id="email" name="email" placeholder="Digite o email" class="form-control" value="{{usuarios.email}}">
            <br>
            <button type="submit" class="btn btn-success">Editar Cliente</button>
             <a href="admin/usuarios"> <button type="submit" class="btn btn-success">voltar</button>
        </form>
    </div>
</div>