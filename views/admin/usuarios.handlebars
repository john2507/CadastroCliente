<h2>Lista de Clientes</h2>
<hr>
<a href="/admin/usuarios/add"> <button class="btn btn-success">Novo Cliente</button></a>
<a href="/admin/usuarios"> <button type="submit" class="btn btn-success">Listar Clientes</button></a>
<a href="/admin/"> <button type="submit" class="btn btn-success">voltar</button></a>
{{#each usuarios}}
<div class="card">
    <div class="card-body">
        <h5>Nome: {{nome}}</h5>
        <h5>Endereço: {{endereco}}</h5>
        <h5>Email: {{email}}</h5>
        <small>Data da Criacao: {{date}}</small><br>
        <a href="/admin/usuarios/edit/{{_id}}"><button class="btn btn-primary mt-4">Editar Clientes</button></a>
         <form action="/admin/usuarios/delete" method="POST">
             <input type="hidden" name="id" value="{{_id}}">

            <button class="btn btn-danger mt-2">Exluir Clientes</button>
         </form>
         
    </div>
    
</div>
{{else}}

<h4 class="mt-4" style="color: brown;">NENHUM CLIENTE CADASTRADO</h4>

{{/each}}